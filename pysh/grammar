line = pipeline? ws? output_redirect?
output_redirect = (">" ws? output_file_path)
output_file_path = ~"[-./\w]+"
file_path = ~"[-./\w]+"
pipeline = command (pipe command)*
pipe = ~" *\| *"
command = verb (ws word)*
verb = builtin / file_path
builtin = "cd"
word = ~"[^ |><]+"
ws = ~" +"
